---
- name: bat
  hosts: localhost
  roles:
    - role: cargo
      vars:
        url: 'https://github.com/sharkdp/bat.git'
        repo_dir: bat
        version: "{{ env_lock['bat'] }}"
        package_name: bat
        build_root: []
  environment:
    - RUSTONIG_SYSTEM_LIBONIG: 1
