---
- name: atuin
  hosts: localhost
  roles:
    - role: cargo
      vars:
        url: 'https://github.com/atuinsh/atuin'
        repo_dir: atuin
        version: "{{ env_lock['atuin'] }}"
        package_name: atuin
        build_root: ['crates', 'atuin']
        completions:
          executable: atuin
          args:
            - gen-completions
            - --shell
            - zsh
